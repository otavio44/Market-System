name: Tests

on: [push]

jobs:
  tests:
    runs-on: ubuntu-latest

    env:
      DB_CONNECTION: mysql
      DB_HOST: webstamp-db
      DB_PORT: 3306
      DB_DATABASE: stpatool
      DB_USERNAME: webstampEmbraer
      DB_PASSWORD: W3b574mp3mbr43R

    steps:
    - uses: actions/checkout@v2
    - name: Build container
      run: docker-compose up -d --build
    - name: Run docker ps -a
      run: docker ps -a
#    - name: Install dependencies 
#      run: docker exec -u 0 webstamp-app composer install -q --ignore-platform-reqs --no-ansi --no-interaction --no-scripts --no-progress --prefer-dist
#    - name: Generate Keys
#      run: docker exec -u 0 webstamp-app php artisan key:generate
#    - name: Run migrations
#      run: docker exec -u 0 webstamp-app php artisan migrate --seed
#    - name: Run Tests
#      run: docker exec -u 0 webstamp-app vendor/bin/phpunit tests/EndToEnd
